<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <f:section title="${%Disable Build Triggers Config}">
    <f:entry title="${%Blacklist of hudson.model.Cause subclasses}" field="blackList">
        <f:textbox/>
    </f:entry>
    <f:entry title="${%suppress configured triggers to schedule builds}" field="suppressTriggers">
        <f:checkbox />
    </f:entry>
  </f:section>
</j:jelly>